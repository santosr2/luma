# git-cliff configuration for Luma
# https://git-cliff.org/docs/configuration

[changelog]
# Template for the changelog body
# https://keats.github.io/tera/docs/#introduction
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% if version and release.notes %}\
    {{ release.notes | trim }}
{% endif %}\
{%- set breaking_commits = commits | filter(attribute="breaking", value=true) -%}
{%- if breaking_commits | length > 0 %}
    ### ⚠️  BREAKING CHANGES
    {% for commit in breaking_commits -%}
        - {% if commit.scope %}**{{ commit.scope }}**: {% endif %}{{ commit.message | upper_first }} ([`{{ commit.id | truncate(length=7, end="") }}`](https://github.com/santosr2/luma/commit/{{ commit.id }})){% if commit.github.username %} by [@{{ commit.github.username }}](https://github.com/{{ commit.github.username }}){% endif %}
        {%- if commit.breaking_description %}
          {{ commit.breaking_description | indent(n=4) }}
        {%- endif %}
    {% endfor %}
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits -%}
        {% if commit.scope -%}
            - **{{ commit.scope }}**: {{ commit.message | upper_first }} ([`{{ commit.id | truncate(length=7, end="") }}`](https://github.com/santosr2/luma/commit/{{ commit.id }})){% if commit.github.username %} by [@{{ commit.github.username }}](https://github.com/{{ commit.github.username }}){% endif %}
        {%- else -%}
            - {{ commit.message | upper_first }} ([`{{ commit.id | truncate(length=7, end="") }}`](https://github.com/santosr2/luma/commit/{{ commit.id }})){% if commit.github.username %} by [@{{ commit.github.username }}](https://github.com/{{ commit.github.username }}){% endif %}
        {%- endif %}
        {%- if commit.body %}
          {{ commit.body | indent(n=4) }}
        {%- endif %}
    {% endfor %}
{% endfor %}\n
"""
# Remove the leading and trailing whitespace from the templates
trim = true
# Changelog footer
footer = """
<!-- generated by git-cliff -->
"""
# Postprocessors for commit messages
postprocessors = [
  # Remove issue numbers from commit messages
  { pattern = '\((\w+\s)?#([0-9]+)\)', replace = "" },
]

[git]
# Parse the commits based on https://www.conventionalcommits.org
conventional_commits = true
# Filter out the commits that are not conventional
filter_unconventional = false
# Process each line of a commit as an individual commit
split_commits = false
# Regex for parsing and grouping commits
commit_parsers = [
  { message = "^feat", group = "<!-- 0 -->✨ Features" },
  { message = "^fix", group = "<!-- 1 -->🐛 Bug Fixes" },
  { message = "^doc", group = "<!-- 2 -->📚 Documentation" },
  { message = "^perf", group = "<!-- 3 -->⚡ Performance" },
  { message = "^refactor", group = "<!-- 4 -->♻️  Refactor" },
  { message = "^style", group = "<!-- 5 -->🎨 Styling" },
  { message = "^test", group = "<!-- 6 -->🧪 Testing" },
  { message = "^chore\\(release\\): prepare for", skip = true },
  { message = "^chore\\(deps.*\\)", skip = true },
  { message = "^chore\\(pr\\)", skip = true },
  { message = "^chore\\(pull\\)", skip = true },
  { message = "^chore|^ci", group = "<!-- 7 -->⚙️  Miscellaneous Tasks" },
  { body = ".*security", group = "<!-- 8 -->🛡️  Security" },
  { message = "^revert", group = "<!-- 9 -->◀️  Revert" },
]
# Protect breaking changes from being skipped due to matching a skipping commit_parser
protect_breaking_commits = false
# Filter out the commits that are not matched by commit parsers
filter_commits = false
# Regex for matching git tags
tag_pattern = "v[0-9].*"
# Sort tags chronologically
date_order = false
# Sort commits inside sections by oldest/newest order
sort_commits = "oldest"
# Limit the number of commits included in the changelog
# limit_commits = 42

[remote.github]
# GitHub repository owner and name
owner = "santosr2"
repo = "luma"

